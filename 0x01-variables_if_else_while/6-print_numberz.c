#include <stdio.h>
/**
  *main-entry point of program
  *Return:0 if no error,non zero-value if errors
**/

int main(void)
{	int c;

	for (c = '0'; c <= '9'; c++)
	{
		putchar (c);
	}
		putchar ('\n');
		return (0);
}
