#include "main.h"
/**
  *add -function adds two integer
  *@m: first number to compute
  *@n: second number to compute
  *Return: result
*/

int add(int m, int n)
{
	int result;

	result = m + n;

	return (result);
}
